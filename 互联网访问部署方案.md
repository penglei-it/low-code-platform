# 🌐 互联网访问部署方案

## 当前状态

您看到的输出：
```
➜  Local:   http://localhost:4173/
➜  Network: http://10.118.39.155:4173/
```

这是 **正常的**，表示：
- ✅ **本地访问**：同一台电脑可以通过 `localhost` 访问
- ✅ **局域网访问**：同一 WiFi/网络的其他设备可以通过 `10.118.39.155:4173` 访问

## 🔒 为什么不能直接互联网访问？

1. **NAT 网络**：大多数家庭/办公网络使用 NAT，没有公网 IP
2. **防火墙**：路由器防火墙会阻止外部访问
3. **ISP 限制**：运营商可能限制端口开放

## 🌐 让互联网用户访问的解决方案

### 方案一：使用内网穿透工具（推荐新手）

#### 1. 使用 ngrok（最简单）

```bash
# 1. 下载 ngrok
# 访问：https://ngrok.com/download
# 或使用 npm
npm install -g ngrok

# 2. 注册账号获取 token（免费）
# 访问：https://dashboard.ngrok.com/get-started/your-authtoken

# 3. 配置 token
ngrok config add-authtoken <你的token>

# 4. 启动前端穿透
ngrok http 4173

# 5. 启动后端穿透（新终端）
ngrok http 3001
```

**优点**：
- ✅ 5分钟即可配置完成
- ✅ 自动获得 HTTPS 域名
- ✅ 免费版本可用

**缺点**：
- ❌ 免费版域名随机，每次启动都变化
- ❌ 有流量限制

#### 2. 使用 Cloudflare Tunnel（免费且稳定）

```bash
# 1. 下载 cloudflared
# Windows: https://github.com/cloudflare/cloudflared/releases

# 2. 登录 Cloudflare
cloudflared tunnel login

# 3. 创建隧道
cloudflared tunnel create low-code-platform

# 4. 配置并运行
cloudflared tunnel run low-code-platform
```

**优点**：
- ✅ 完全免费
- ✅ 域名稳定
- ✅ HTTPS 自动配置
- ✅ 无流量限制

#### 3. 使用 frp（自建服务器）

如果您有自己的服务器，可以使用 frp：

```bash
# 服务器端（您的 VPS）
# frps.ini
[common]
bind_port = 7000

# 客户端（本地）
# frpc.ini
[common]
server_addr = 你的服务器IP
server_port = 7000

[web_frontend]
type = http
local_port = 4173
custom_domains = your-domain.com

[web_backend]
type = tcp
local_ip = 127.0.0.1
local_port = 3001
remote_port = 3001
```

### 方案二：云服务器部署（推荐生产环境）

#### 使用 Docker Compose（最简单）

1. **购买云服务器**（阿里云、腾讯云、AWS 等）

2. **上传项目代码**

3. **运行部署**：
   ```bash
   docker-compose up -d
   ```

4. **配置域名和 SSL**
   - 购买域名
   - 配置 DNS 解析
   - 使用 Let's Encrypt 免费 SSL

#### 手动部署到云服务器

1. **连接服务器**
   ```bash
   ssh username@your-server-ip
   ```

2. **安装 Node.js**
   ```bash
   # Ubuntu/Debian
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

3. **克隆/上传项目**
   ```bash
   git clone <项目地址>
   cd low_coding_platform
   ```

4. **部署**
   ```bash
   # 构建
   cd backend && npm install && npm run build
   cd ../frontend && npm install && npm run build
   
   # 启动（使用 PM2）
   npm install -g pm2
   pm2 start backend/dist/index.js --name backend
   pm2 start npm --name frontend -- run preview --prefix frontend
   ```

5. **配置 Nginx 反向代理**
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;
       
       location / {
           proxy_pass http://localhost:4173;
       }
       
       location /api {
           proxy_pass http://localhost:3001;
       }
   }
   ```

### 方案三：使用 Vercel / Netlify（前端托管）

**前端部署到 Vercel**：
```bash
npm install -g vercel
cd frontend
vercel
```

**后端使用 Railway / Render**：
- Railway: https://railway.app
- Render: https://render.com

这些平台提供免费套餐，适合小型项目。

### 方案四：配置端口转发（需要公网 IP）

如果您有公网 IP：

1. **路由器配置端口转发**
   - 登录路由器管理界面
   - 配置端口转发：
     - 外部端口 80 → 内网 IP:4173（前端）
     - 外部端口 3001 → 内网 IP:3001（后端）

2. **防火墙配置**
   - 开放路由器防火墙端口 80 和 3001

3. **访问**
   - `http://您的公网IP:80`（前端）
   - `http://您的公网IP:3001`（后端 API）

## 🚀 快速开始（推荐）

### 最快方案：使用 ngrok

```bash
# 1. 安装 ngrok
npm install -g ngrok

# 2. 启动前端穿透
ngrok http 4173

# 会得到类似：https://abc123.ngrok.io
# 将这个地址分享给其他人即可访问
```

### 生产环境方案：Docker + 云服务器

```bash
# 在云服务器上
git clone <项目地址>
cd low_coding_platform
docker-compose up -d

# 配置域名指向服务器 IP
# 使用 Nginx + Let's Encrypt 配置 HTTPS
```

## 📋 各方案对比

| 方案 | 难度 | 成本 | 稳定性 | 适用场景 |
|------|------|------|--------|----------|
| ngrok | ⭐ 简单 | 免费 | ⭐⭐ | 临时演示、测试 |
| Cloudflare Tunnel | ⭐⭐ 中等 | 免费 | ⭐⭐⭐⭐ | 个人项目、小型应用 |
| 云服务器 | ⭐⭐⭐ 复杂 | 付费 | ⭐⭐⭐⭐⭐ | 生产环境、正式项目 |
| Vercel/Railway | ⭐⭐ 中等 | 免费/付费 | ⭐⭐⭐⭐ | 快速部署、前端托管 |

## 🎯 推荐方案

### 临时/测试场景
→ 使用 **ngrok**（5分钟搞定）

### 个人项目/演示
→ 使用 **Cloudflare Tunnel**（免费且稳定）

### 正式生产环境
→ 使用 **云服务器 + Docker**（最稳定）

---

**现在您可以通过以下方式让互联网用户访问：**

1. **快速方案**：安装 ngrok，运行 `ngrok http 4173`
2. **稳定方案**：使用 Cloudflare Tunnel
3. **生产方案**：部署到云服务器

需要我帮您配置哪种方案？

