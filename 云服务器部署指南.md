# 🖥️ 云服务器部署指南

如果您想要最稳定的生产环境部署，推荐使用云服务器。

## 选择云服务器

### 推荐平台

1. **阿里云** - https://www.aliyun.com
   - 国内访问速度快
   - 首次购买优惠

2. **腾讯云** - https://cloud.tencent.com
   - 价格优惠
   - 适合国内用户

3. **AWS** - https://aws.amazon.com
   - 全球可用
   - 功能强大

4. **Vercel / Railway** - https://vercel.com / https://railway.app
   - 部署简单
   - 免费套餐

## 部署步骤

### 方案一：使用 Docker Compose（推荐）

```bash
# 1. 连接服务器
ssh username@your-server-ip

# 2. 安装 Docker
# Ubuntu/Debian
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# 3. 安装 Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 4. 克隆项目
git clone <项目地址>
cd low_coding_platform

# 5. 启动服务
docker-compose up -d

# 6. 查看日志
docker-compose logs -f
```

### 方案二：手动部署

```bash
# 1. 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 2. 安装 PM2
sudo npm install -g pm2

# 3. 克隆项目
git clone <项目地址>
cd low_coding_platform

# 4. 构建项目
cd backend && npm install && npm run build
cd ../frontend && npm install && npm run build

# 5. 初始化数据库
cd backend
npx prisma generate
npx prisma migrate deploy

# 6. 启动服务
pm2 start backend/dist/index.js --name backend
pm2 start npm --name frontend -- run preview --prefix frontend -- --host 0.0.0.0
pm2 save
pm2 startup
```

### 方案三：使用 Nginx 反向代理

```bash
# 1. 安装 Nginx
sudo apt-get update
sudo apt-get install nginx

# 2. 配置 Nginx
sudo nano /etc/nginx/sites-available/low-code-platform
```

Nginx 配置：
```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端
    location / {
        proxy_pass http://localhost:4173;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # 后端 API
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

```bash
# 3. 启用配置
sudo ln -s /etc/nginx/sites-available/low-code-platform /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

# 4. 配置 SSL（Let's Encrypt）
sudo apt-get install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

## 配置防火墙

```bash
# Ubuntu/Debian
sudo ufw allow 22/tcp   # SSH
sudo ufw allow 80/tcp   # HTTP
sudo ufw allow 443/tcp  # HTTPS
sudo ufw enable
```

## 监控和维护

### 使用 PM2 监控

```bash
pm2 list          # 查看进程
pm2 logs          # 查看日志
pm2 monit         # 监控面板
pm2 restart all   # 重启所有进程
```

### 设置自动重启

```bash
pm2 startup
pm2 save
```

## 域名配置

1. **购买域名**（阿里云、腾讯云、GoDaddy 等）

2. **配置 DNS 解析**
   - A 记录：指向服务器 IP 地址
   - 或 CNAME：指向服务器域名

3. **等待 DNS 生效**（通常几分钟到几小时）

4. **配置 SSL 证书**（使用 Let's Encrypt 免费证书）

## 成本估算

### 小型项目（推荐配置）

- **服务器**：2核2G，约 ¥50-100/月
- **域名**：约 ¥50-100/年
- **SSL**：免费（Let's Encrypt）
- **总计**：约 ¥50-100/月

### 免费方案

- **Vercel**：前端免费托管
- **Railway**：后端免费额度
- **Cloudflare**：免费 CDN 和 Tunnel
- **总计**：完全免费（有使用限制）

